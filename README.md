# Tankri OCR Project

This project has been reorganized into the following structure:

- configs/ for experiment YAML files
- dataset/ for raw and processed data
- models/ for trained checkpoints
- outputs/ for generated artifacts
- src/ for the training and inference pipeline
- notebooks/ for experiments

Use the existing notebooks, dataset folders, and DVC/MLflow files as-is.
